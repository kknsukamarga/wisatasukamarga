/**
 * Public routes
 * @type {string[]}
 */

export const publicRoutes = [
  "/",
  "/sign-in",
  "/wisata",
  "/umkm",
  "/blogs",
  "/peta-interaktif",
  "/api/blog",
];

export const authRoutes = ["/sign-in"];

export const apiAuthPrefix = "/api/auth";
